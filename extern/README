Transmission: removed for performance reasons
./autogen.sh
./configure --disable-nls --without-gtk --enable-cli --enable-daemon --prefix=<where you want to download>

can exclude --prefix if u want default installation dirs (installing to /)

When in doubt, download fresh source (ideal) or svn co (not recommanded)

------------------------------------------------------------------------

7z source file structure changed completely.
Currently would only compile under linux

! DO NOT CHANGE STRUCTURE !

If not compiling, check if you are using the right Defs.h
The Windows one is called DefsW.h

-----------------------------------------------------------------------

ZSTD
READ THIS PLOXo

to compile the the extern zstd lib do:
make lib-mt
then copy the static library (libzstb.a) over to <project_root>/lib